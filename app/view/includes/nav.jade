nav.navbar.navbar-inverse.navbar-fixed-top
  .container-fluid
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') DataSift Quota System
    .navbar-collapse.collapse
      ul.navbar-nav.nav
        li
          a(href='/') Home
        //li.disabled
          a(href='#about') About
        //li.dropdown
          a.dropdown-toggle(href='#', data-toggle='dropdown')
            | Quick Menu
            b.caret
          ul.dropdown-menu(role='menu')
        li
          a(href='/datasift/stream') CSDL Editor
        li
          a(href='/profile') Profile
      ul.navbar-right.nav.navbar-nav
        if user == null                  
          li.active
            a(href='/login') Sign in
        else
          li.active
            a(href='/profile')
              = (user.first_name?user.first_name:'') + ' (' + user.email + ')'
          li.active
            a(href='/profile')
              = 'Credit: ' + (user.quota.cap - user.quota.used) + ' DPU'
          li 
            a(href="/logout") Logout
