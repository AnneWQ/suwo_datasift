if info
  for msg in info
    .alert.alert-success
      button.close(type='button' data-dismiss='alert') &times;
      = msg
if error
  for msg in error
    .alert.alert-danger
      button.close(type='button' data-dismiss='alert') &times;
      strong Error! 
      = msg
